<Window x:Class="TaskPlanning.App.AddTaskWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TaskPlanning.App"
        mc:Ignorable="d"
       Title="Задача" Height="360" Width="480" 
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="Создание новой задачи" FontSize="18" Margin="10,0,0,15"/>
        <TextBlock Grid.Row="1" Text="Название" Margin="10,0,0,4" FontSize="12" VerticalAlignment="Center"/>
        <TextBox Grid.Row="2" x:Name="TitleTextBox" Height="26" Margin="10,0,10,12" 
                 Background="White" BorderBrush="Black" VerticalContentAlignment="Center" Padding="5,0"/>
        <TextBlock Grid.Row="3" Text="Описание" Margin="10,0,0,4" FontSize="12" VerticalAlignment="Center"/>
        <TextBox Grid.Row="4" x:Name="DescriptionTextBox" Height="70" Margin="10,0,10,15" 
                 Background="White" BorderBrush="Black" TextWrapping="Wrap" AcceptsReturn="True" Padding="5"/>
        <Grid Grid.Row="5" Margin="10,0,10,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="130"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="130"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Column="0" Grid.Row="0" Text="Срок" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <DatePicker Grid.Column="1" Grid.Row="0" x:Name="DueDatePicker" Height="26" 
                        Background="White" BorderBrush="Black" VerticalContentAlignment="Center"/>
            <TextBlock Grid.Column="2" Grid.Row="0" Text="Тег" VerticalAlignment="Center" Margin="15,0,10,0"/>
            <TextBox Grid.Column="3" Grid.Row="0" x:Name="TagTextBox" Height="26"
         Background="White" BorderBrush="Black" VerticalContentAlignment="Center" Padding="5,0"/>
            <Button Grid.Column="4" Grid.Row="1" Content="Добавить задачу" x:Name="SaveButton"
                    Click="SaveButton_Click" Width="130" Height="28" 
                    HorizontalAlignment="Right" VerticalAlignment="Bottom" 
                    Background="White"  IsDefault="True"/>
        </Grid>
    </Grid>
</Window>