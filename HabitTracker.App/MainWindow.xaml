<Window x:Class="HabitTracker.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HabitTracker.App"
        mc:Ignorable="d"
        Title="Главное окно" Height="450" Width="600" WindowStartupLocation="CenterScreen">
    <Grid Margin="15">
        <Grid.RowDefinitions>
           
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="180"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" 
                       Text="{Binding CurrentPerson.Name, FallbackValue='Пользователь 1'}" 
                       FontSize="20" VerticalAlignment="Top" Margin="20,10,0,0"/>
            <StackPanel Grid.Column="1" VerticalAlignment="Top">
                <Button Content="Добавить задачу" 
                        Command="{Binding AddTaskCommand}" 
                        Height="30" Margin="0,0,0,8" Background="White" BorderBrush="Black"/>
                <Button Content="Редактировать задачу" 
                        Command="{Binding EditTaskCommand}" 
                        CommandParameter="{Binding SelectedTask}" 
                        Height="30" Background="White" BorderBrush="Black"/>
            </StackPanel>
        </Grid>
        <Grid Grid.Row="1" Margin="20,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="220"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="180"/>
            </Grid.ColumnDefinitions>
            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="60"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Row="0" Grid.Column="0" Text="Теги" VerticalAlignment="Center" Margin="0,0,0,8"/>
                <ComboBox Grid.Row="0" Grid.Column="1" 
                          ItemsSource="{Binding AvailableTags}" 
                          SelectedItem="{Binding SelectedTag, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"    DisplayMemberPath="Name"
                          Height="26" Margin="0,0,0,8" Background="White" BorderBrush="Black"/>
                <TextBlock Grid.Row="1" Grid.Column="0" Text="Период" VerticalAlignment="Center"/>
                <ComboBox Grid.Row="1" Grid.Column="1" 
                          ItemsSource="{Binding Periods}" 
                          SelectedItem="{Binding SelectedPeriod}"
                          Height="26" Background="White" BorderBrush="Black"/>
            </Grid>
            <StackPanel Grid.Column="1" Margin="20,0,20,0" VerticalAlignment="Bottom">
                <TextBlock Text="Прогресс" Margin="0,0,0,4" HorizontalAlignment="Left"/>
                <ProgressBar Height="20" Minimum="0" Maximum="100" 
                             Value="{Binding Progress, Mode=OneWay}" 
                             Background="White" BorderBrush="Black" Foreground="LightGray"/>
            </StackPanel>
            <Button Grid.Column="2" Content="Удалить задачу" 
                    Command="{Binding DeleteTaskCommand}" 
                    CommandParameter="{Binding SelectedTask}"
                    Height="30" VerticalAlignment="Bottom" Background="White" BorderBrush="Black"/>
        </Grid>
        <Border Grid.Row="2" BorderBrush="Black" BorderThickness="1" Margin="10,0,10,10">
            <ListBox ItemsSource="{Binding Tasks}" 
                     SelectedItem="{Binding SelectedTask, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                     BorderThickness="0" Background="Transparent" HorizontalContentAlignment="Stretch" Padding="10">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="{Binding Title, FallbackValue='Название задачи'}" VerticalAlignment="Center" Margin="5,0,0,0"/>
                            <CheckBox Grid.Column="1" VerticalAlignment="Center" Margin="0,0,5,0"
          IsChecked="{Binding IsCompleted, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
          Command="{Binding DataContext.ToggleTaskCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"/>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>
    </Grid>
</Window>


